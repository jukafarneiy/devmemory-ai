{
  "name": "devmemory-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first persistent memory for AI-assisted software projects.",
  "license": "SEE LICENSE IN LICENSE.md",
  "repository": {
    "type": "git",
    "url": "https://github.com/jukafarneiy/devmemory-ai.git"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "prepackage:vscode": "npm run build && npm run test",
    "package:vscode": "npm run package -w apps/vscode-extension && npm run verify:vscode-package",
    "verify:vscode-package": "node scripts/verify-vsix.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "vitest": "^4.1.5",
    "yauzl": "^3.2.0"
  }
}
