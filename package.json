{
  "name": "devmemory-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first persistent memory for AI-assisted software projects.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "vitest": "^4.1.5"
  }
}
