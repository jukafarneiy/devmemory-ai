{
  "name": "@devmemory/core",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first memory core for DevMemory AI.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "devmemory": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run src/**/*.test.ts"
  }
}
